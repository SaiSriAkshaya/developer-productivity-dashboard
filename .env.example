# This project is currently frontend-only and uses mock data (src/data/mockData.js),
# so no environment variables are required yet.
#
# Once Task 2's API is live, point the dashboard at it like this:
# VITE_API_BASE_URL=http://localhost:4000/api
